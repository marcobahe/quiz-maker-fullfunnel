{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && npm run typecheck && next build",
  "installCommand": "npm ci",
  "regions": ["gru1"],
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true
    }
  }
}
