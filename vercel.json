{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push --skip-generate && next build",
  "installCommand": "npm ci",
  "regions": ["gru1"],
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true
    }
  }
}
